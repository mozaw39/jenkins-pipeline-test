pipeline {

    agent any

    stages {

        stage("build"){

            steps {
                echo 'bulding the app'
            }
        }

        stage("test"){

            steps {
                echo 'testing the app'
            }
        }

        stage("test"){

            steps {
                echo 'testing the app'
            }
        }

    }
}